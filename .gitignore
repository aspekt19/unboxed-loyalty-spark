# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local Deno cache lock for supabase/functions — not used by CI or the web build
deno.lock

# Env — keep secrets and local overrides out of git (.env.example is tracked)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local ops — never commit plaintext or decrypt keys
scripts/.admin-wallets.local.json
scripts/.admin-wallets-decrypt.key
supabase/functions/_shared/bot-cluster-wallets.json

# Local fundraising notes / application copy — private, never commit
docs/fundraising/

# Lovable working files — plan drafts, not part of the app
.lovable/plan.md
.lovable/plan/
